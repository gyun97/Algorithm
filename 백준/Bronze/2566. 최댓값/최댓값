import sys

matrix = [list(map(int, sys.stdin.readline().split())) for _ in range(9)]

max_num = -1

for i in range(9):
    if max_num < max(matrix[i]):
        max_num = max(matrix[i])
        max_row = i + 1
        max_col = matrix[i].index(max_num) + 1

print(max_num)
print(max_row, max_col)
